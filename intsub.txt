int sub(int x, int y)
{
    // Iterate till there is no carry
    y = y ^ #0xFFFFFFFF //2's compliment
    return add(x, y)
}

intsub:
		push {r2-r4}
		mov r2, r0 //r2 = x
		mov r3, r1 //r3 = y
		mov r4, #0 //r4 = ret

		eor r3, r3, #0xFFFFFFFF
		mov r0, r2
		mov r1, r3
		bl intadd

		mov pc, lr